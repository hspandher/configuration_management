[uwsgi]
ini = /etc/uwsgi/vassals/common_config
socket = /tmp/%n.sock
chdir = {{project_path}}
pidfile = /tmp/%n.pid
wsgi-file = deploy/uwsgi.py
processes = 20
virtualenv = {{ virtualenv_path }}
logger = syslog:can-uwsgi,local7
